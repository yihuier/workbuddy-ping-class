# Ping Class 本地开发补充示例

复制为已忽略的 `AGENTS.local.md` 后填写本机信息。不要在这里保存 Token、密钥或 databaseId；个人 databaseId 只写入 `workbuddy.local.json`。

- 产品参考 HTML：`<absolute-path-to-reference-page>`
- 可选旧版开发快照：`<absolute-path-to-local-snapshot>`
- WorkBuddy skill-library：优先设置环境变量 `WORKBUDDY_SKILL_LIBRARY=<absolute-path>`

上述资源只读，仅用于本地行为与契约比对。
